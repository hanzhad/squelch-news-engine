name: classify

on:
  schedule:
    # Ten minutes behind scrape, so it always finds the issues that run created.
    - cron: "10,40 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: classify
  cancel-in-progress: false

jobs:
  classify:
    runs-on: ubuntu-latest
    # Shorter than its own 30-minute cadence, so runs never queue up behind a
    # non-cancelling concurrency group.
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: pip install -e .

      - name: Classify
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: squelch classify
